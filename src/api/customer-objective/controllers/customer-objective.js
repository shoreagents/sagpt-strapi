'use strict';

/**
 * customer-objective controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::customer-objective.customer-objective');
