'use strict';

/**
 * customer-objective router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::customer-objective.customer-objective');
